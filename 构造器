class Point{
	double x,y;
	public Point(double _x,double _y) {//构造方法名称必须和类名保持一致；
		x = _x;
		y = _y;
		return;//构造器虽然有返回值，但是不能定义返回值类型（返回值的类型肯定是本类），不能再构造器里使用return返回某个值。
	}
	public double getDistance( Point p) {//这里的p是属于getDistance方法；
		
		return Math.sqrt((x-p.x)*(x-p.x)+(y-p.y)*(y-p.y));
	}
	Point(){//如果我们没有定义构造器，编译器会自动构造一个无参的构造函数，如果已定义则编译器不会添加；构造器的方法必须和类名保持一致
		//对象的创建不完全是构造方法实现的，构建方法是创建java对象的重要途径，通过new关键字调用构造器时，构造器确实返回了该类
		//对象，但这个对象并不完全是由构造器负责创建的，创建一个对象分为：分配对象空间，并初始化成员变量为0或空；执行属性值的显式
		//初始化；执行构造方法；返回对象的值给相关变量。
	}
}
